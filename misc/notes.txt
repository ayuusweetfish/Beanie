1.
(a) What is the maximum possible value of any element of `A`, at any time throughout the program?
(b) Assuming that `char` equals `int8_t`, what is the maximum value of `N` that the program correctly supports?

2.
(a) Under which values of `N` can the player be sure to trigger a row-2 move?
(b) Under which values of `N` can the player be sure to trigger a deep move on the first try, or determine that it is not possible, during play?
c = Y / 2
r0 = Y + (Y & N(N-1))
explode = Y & 4
Given c, we can infer the behaviour iff there is an r0 s.t. (c, r0, 0) for any r0' != r0, and not (c, r0, 1) for all r0' != r0.

3.
Win the game in 2 moves.

cc prog.c -o /tmp/a.out && /tmp/a.out
cc prog.c -o /tmp/a.out -g -fsanitize=address,undefined,bounds-strict -include stdio.h && /tmp/a.out
cc prog.c -o /tmp/a.out -g -fsanitize=address,undefined,bounds-strict -include stdio.h && /tmp/a.out >(sox --buffer 1024 -t s16 -r 48000 -c 1 - -d 2>/dev/null)
clang fuzz.c -o /tmp/a.out -g -O2 -fsanitize=fuzzer,address,undefined && /tmp/a.out
clang fuzz.c -o /tmp/a.out -g -O2 -fsanitize=fuzzer,address,undefined -include stdio.h -DPRINT && /tmp/a.out ./crash-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ln -sf $PWD/prog.c arduino/serial_play/prog.h
